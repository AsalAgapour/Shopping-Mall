# 🛒Shopping Mall

این پروژه یک شبیه‌ساز کامل فروشگاه اینترنتی است که با استفاده از زبان برنامه‌نویسی **جاوا (Java)** و کتابخانه گرافیکی **Swing** توسعه داده شده است. هدف اصلی این پروژه پیاده‌سازی مفاهیم بنیادین شی‌ءگرایی (OOP)، مدیریت داده‌ها با فایل (File I/O) و طراحی رابط کاربری گرافیکی (GUI) بدون استفاده از فریم‌ورک‌های پیچیده است.

---

---

## 📖 معرفی پروژه

این نرم‌افزار امکان مدیریت محصولات توسط ادمین و تجربه خرید کامل توسط مشتری را فراهم می‌کند. داده‌ها به صورت دائمی در فایل‌های CSV ذخیره می‌شوند، بنابراین با بسته شدن برنامه اطلاعات از بین نمی‌روند. سیستم دارای دو پنل مجزا برای **ادمین** و **مشتری** است که از طریق یک سیستم لاگین امن قابل دسترسی هستند.

---

## ✨ ویژگی‌های کلیدی

### 👤 پنل ادمین (Administrator)
*   **مدیریت محصولات:** قابلیت ایجاد، خواندن، ویرایش و حذف محصولات.
*   **مدیریت موجودی:** مشاهده لحظه‌ای تعداد کالاها در انبار.
*   **آپلود تصویر:** امکان انتخاب تصویر برای محصولات جدید از طریق File Chooser.
*   **ویرایش هوشمند:** با کلیک روی هر محصول در جدول، اطلاعات آن جهت ویرایش بارگذاری می‌شود.

### 🛍️ پنل مشتری (Customer)
*   **نمایش ویترینی:** مشاهده محصولات به صورت کارت‌های گرافیکی همراه با تصویر و قیمت.
*   **جستجو (Search):** قابلیت جستجوی دینامیک محصولات بر اساس نام.
*   **سبد خرید (Cart):** افزودن کالا به سبد خرید و مشاهده لیست اقلام انتخابی.
*   **پرداخت نهایی (Checkout):** 
    *   محاسبه مجموع قیمت.
    *   بررسی اعتبار کیف پول مشتری.
    *   کسر اتوماتیک موجودی کالا از انبار اصلی پس از خرید موفق.

### ⚙️ ویژگی‌های سیستم
*   **سیستم لاگین:** احراز هویت کاربران بر اساس نام کاربری و رمز عبور.
*   **ذخیره‌سازی فایل:** تمام تغییرات (کاربران و محصولات) در فایل‌های `users.csv` و `products.csv` ذخیره می‌شوند.
*   **رابط کاربری واکنش‌گرا:** استفاده از `CardLayout` برای جابجایی روان بین صفحات بدون باز شدن پنجره‌های متعدد.

---

## 🛠 تکنولوژی‌ها و مفاهیم به کار رفته

در توسعه این پروژه بر پیاده‌سازی "دستی" و درک عمیق مفاهیم تمرکز شده است:

*   **زبان:** Java SE (Standard Edition)
*   **رابط کاربری:** Java Swing (JFrame, JPanel, JButton, JTable, CardLayout, ScrollPane)
*   **شی‌ءگرایی (OOP):**
    *   **Inheritance:** کلاس‌های `Admin` و `Customer` از کلاس والد `User` ارث‌بری می‌کنند.
    *   **Polymorphism:** رفتار متفاوت سیستم بر اساس نوع کاربر لاگین شده.
    *   **Encapsulation:** استفاده از فیلدهای private و متدهای getter/setter.
*   **مدیریت داده:** کار با فایل‌های متنی (Text Processing)، پارس کردن CSV، استفاده از `Scanner` و `FileWriter`.
*   **ساختمان داده:** استفاده گسترده از `ArrayList` برای مدیریت لیست‌های پویا.
*   **الگوریتم:** جستجوی خطی، فیلترینگ داده‌ها، و منطق کسر موجودی.

---

## 📂 ساختار فایل‌ها و معماری

ساختار پروژه بر اساس تفکیک مسئولیت‌ها طراحی شده است:

| نام فایل | توضیحات |
| :--- | :--- |
| `Main.java` | نقطه شروع برنامه (Entry Point). |
| `MainGUI.java` | **قلب گرافیکی برنامه.** مدیریت تمام صفحات، رویدادها (Events) و اتصال به منطق برنامه. |
| `User.java` | کلاس والد برای کاربران (Abstract یا Base Class). |
| `Admin.java` | کلاس مدل مربوط به مدیران سیستم. |
| `Customer.java` | کلاس مدل مشتریان (شامل کیف پول و متدهای خرید). |
| `Product.java` | کلاس مدل محصول (شناسه، نام، قیمت، موجودی، تصویر). |
| `FileStorage.java` | کلاس اختصاصی برای خواندن و نوشتن در فایل‌های CSV. |
| `users.csv` | دیتابیس متنی کاربران. |
| `products.csv` | دیتابیس متنی محصولات. |
| `img/` | پوشه حاوی تصاویر محصولات. |

---
اطلاعات ورود:admin/admin برای ادمین ، user/user  برای مشتری


نحوه استفاده

سناریوی ادمین:
با اکانت ادمین وارد شوید.
برای افزودن محصول: فیلدها را پر کنید، عکس را انتخاب کنید و دکمه Add را بزنید.
برای ویرایش: روی ردیف محصول در جدول کلیک کنید، تغییرات را اعمال کرده و Update Selected را بزنید.
برای حذف: محصول را انتخاب و Delete Selected را بزنید.

سناریوی مشتری:
با اکانت مشتری وارد شوید.
لیست محصولات را مرور کنید یا از نوار Search استفاده کنید.
دکمه Add to Cart را برای محصولات مورد نظر بزنید.
به تب Cart بروید و دکمه Checkout را بزنید. اگر موجودی کافی باشد، خرید انجام شده و از انبار کسر می‌شود.


📸تصاویر محیط برنامه:


صفحه لاگین:


<img width="1183" height="946" alt="Screenshot (631)" src="https://github.com/user-attachments/assets/8ba4430b-b529-4afe-8c17-835f93fd5ae4" />
پنل ادمین:



<img width="1209" height="974" alt="Screenshot (634)" src="https://github.com/user-attachments/assets/aae4fab3-241d-4fa1-a4ad-c5809172e81c" />


پنل مشتری:


<img width="1205" height="970" alt="Screenshot (633)" src="https://github.com/user-attachments/assets/5bec73a2-f3c0-4250-b617-81e63ab70a61" />

مشارکت کنندگان:
-عسل آقاپور
-آبلار حسین نژاد


