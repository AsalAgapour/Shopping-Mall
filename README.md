# Shopping-Mall
این پروژه یک اپلیکیشن دسکتاپ برای مدیریت یک مرکز خرید مجازی است که با زبان Java و کتابخانه Swing طراحی شده است. برنامه دارای دو سطح دسترسی مختلف برای مدیران (Admin) و مشتریان (Customer) می‌باشد.
📝 توضیحات کلی برنامه
این سیستم به کاربران اجازه می‌دهد تا به عنوان مشتری وارد شده، محصولات را جستجو کنند، به سبد خرید اضافه نمایند و خرید خود را نهایی کنند. همچنین مدیران سیستم امکان مدیریت کامل موجودی کالاها (افزودن، ویرایش و حذف) را دارا هستند. تمامی داده‌ها برای پایداری در یک فایل متنی ذخیره می‌شوند.
اطلاعات ورود پیش‌فرض:
• مدیر: نام کاربری: admin | رمز عبور: admin
• مشتری: نام کاربری: user | رمز عبور: user (موجودی اولیه: 100$)
🏗 ساختار کلاس‌ها و مسئولیت‌ها
برنامه از اصول شیءگرایی (OOP) پیروی می‌کند:
بخش مدل‌سازی (Models)
• User (Abstract Class): کلاس والد برای کاربران که ویژگی‌های مشترک (نام کاربری و رمز عبور) را تعریف می‌کند.
• Admin & Customer: کلاس‌های فرزند که از User ارث‌بری کرده و رفتارهای خاص خود را دارند (مثلاً موجودی برای مشتری).
• Product: مسئول نگهداری اطلاعات هر کالا شامل کد، نام، قیمت، موجودی و آدرس تصویر.
• Cart: مسئول مدیریت سبد خرید موقت مشتری و محاسبه قیمت کل.
بخش مدیریت داده (Data Management)
• DataStorage (Interface): یک رابط برای تعریف متدهای ذخیره و بازیابی، جهت رعایت اصل Dependency Inversion.
• FileService: پیاده‌سازی اینترفیس برای کار با فایل متنی (products.txt) و مدیریت ذخیره‌سازی داده‌ها.
بخش رابط کاربری (GUI)
• MainGUI: کلاس اصلی گرافیکی که از CardLayout برای جابجایی بین صفحات (Login, Admin, Customer) استفاده می‌کند و تمامی رویدادها (Events) را مدیریت می‌کند.
• Main: نقطه شروع (Entry Point) برنامه.
👥 اعضای تیم پروژه (Team Members)
• آیلار حسین‌نژاد (Ailar Hossein-Nejad)
• عسل آقاپور (Asal Aghapour)
🛠 تکنولوژی‌های استفاده شده
• Language: Java
• Library: Swing (GUI), AWT
• Persistence: File I/O (Scanner/FileWriter)
[English Summary]
A Java-based Shopping Mall application featuring a dual-user system (Admin/Customer), CRUD operations for products, a persistent file-based database, and a dynamic GUI built with Swing.
